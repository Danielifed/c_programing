#include <stdio.h>
#include <stdlib.h>

//This program finds the sum of a given number, nth times

int main(void)
{
    int n, i, sum;
    printf("%d input the number to be added: ");
    scanf("%d", & n);

    sum = 0;
    for (i )
}